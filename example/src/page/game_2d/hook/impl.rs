use super::*;

impl PartialEq for BallStore {
    fn eq(&self, _other: &Self) -> bool {
        true
    }
}

impl PartialEq for CanvasCache {
    fn eq(&self, _other: &Self) -> bool {
        true
    }
}
